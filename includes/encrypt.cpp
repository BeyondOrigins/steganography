#include <fstream>
#include <iostream>
#include "encrypt.h"

#include <string>

using namespace std;

namespace stg {
  fstream encrypt(char* key_path, char* data_path, FileData& result) {
    
  }
}
